// Thin route wrapper — the implementation lives in the events feature module.
// See src/features/events for types, service, hooks, and components.
export { EventsAdminPage as default } from "@/features/events";
