# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false
# Hoist to a flat node_modules for maximum compatibility with the existing
# Vite + three.js + firebase toolchain (avoids phantom-dependency breakage
# during the npm -> pnpm migration).
shamefully-hoist=true
